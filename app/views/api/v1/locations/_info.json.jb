json = {
  id: location.id,
  name: location.name,
  address: location.address,
  lat: location.lat,
  lng: location.lng,
  city: location.city,
  zipcode: location.zipcode,
  description: location.description
}

json[:image_url] = polymorphic_url(location.image) if location.image.attached?

json
