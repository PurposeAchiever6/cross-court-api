json = {
  id: location.id,
  name: location.name,
  direction: location.direction,
  lat: location.lat,
  lng: location.lng,
  city: location.city,
  zipcode: location.zipcode
}

json[:image_url] = url_for(location.image) if location.image.attached?

json
