{
  session: render(
    partial: 'info', locals: {
      session: selected_session,
      referee: referee,
      sem: sem,
      user_session: @user_session,
      full: selected_session.full?(date),
      spots_left: selected_session.spots_left(date)
    }
  )
}
